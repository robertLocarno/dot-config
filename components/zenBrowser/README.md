# Zen Browser

I use Zen Browser by default, but a few things need to be changed:

1. Bind toggling the sidebar to `Option + <Space>` and `Option + Shift <Space>` respectively.
2. Install 1Password, UBlock, and Tridactyl.
3. [Disable split mode](https://github.com/zen-browser/desktop/discussions/6331).

